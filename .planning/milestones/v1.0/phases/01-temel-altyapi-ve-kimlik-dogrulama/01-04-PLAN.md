---
phase: 01-temel-altyapi-ve-kimlik-dogrulama
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - "src/app/admin/page.tsx"
  - "middleware.ts"
  - "src/lib/supabase/server.ts"
autonomous: true
requirements: [AUTH-01]
must_haves:
  truths:
    - "Unauthenticated users are redirected from `/admin` to `/login`."
    - "Authenticated users can successfully access the `/admin` page."
    - "The admin page displays information about the logged-in user."
  artifacts:
    - path: "middleware.ts"
      provides: "Server-side route protection logic."
    - path: "src/app/admin/page.tsx"
      provides: "A basic admin dashboard accessible only to authenticated users."
    - path: "src/lib/supabase/server.ts"
      provides: "A Supabase client instance for use in server-side contexts like middleware and Server Components."
  key_links:
    - from: "middleware.ts"
      to: "supabase.auth.getUser()"
      via: "Session check on incoming requests"
      pattern: "supabase.auth.getUser"
    - from: "src/app/admin/page.tsx"
      to: "supabase.auth.getUser()"
      via: "Server Component data fetching"
      pattern: "supabase.auth.getUser"
---

<objective>
Yalnızca kimliği doğrulanmış kullanıcıların erişebileceği korumalı bir yönetici paneli rotası oluşturmak.

Purpose: Bu plan, kimlik doğrulama sistemini pratiğe dökerek projenin ilk korumalı alanını yaratır. Next.js Middleware kullanarak sunucu tarafında rota koruması sağlamak, hassas veya yöneticiye özel içeriğin güvenliğini garanti altına alır.
Output: Bir `/admin` sayfası ve bu sayfayı koruyan bir middleware dosyası.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-temel-altyapi-ve-kimlik-dogrulama/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sunucu Taraflı Supabase İstemcisi Oluştur</name>
  <files>
    - "src/lib/supabase/server.ts"
  </files>
  <action>
    Next.js'in sunucu ortamlarında (Middleware, Route Handlers, Server Components) kullanılmak üzere bir Supabase istemcisi oluştur. `@supabase/ssr` paketini kullan. Bu istemci, istek objesinden cookie'leri okuyup yazabilmelidir. Bu, sunucu tarafında kullanıcının oturum durumunu doğru bir şekilde alabilmek için kritik öneme sahiptir.
  </action>
  <verify>
    <automated>test -e "src/lib/supabase/server.ts" && grep -q "@supabase/ssr" "src/lib/supabase/server.ts"</automated>
  </verify>
  <done>
    Sunucu tarafı işlemleri için yapılandırılmış bir Supabase istemcisi oluşturulmuştur.
  </done>
</task>

<task type="auto">
  <name>Task 2: Korumalı Rota için Middleware Ekle</name>
  <files>
    - "middleware.ts"
  </files>
  <action>
    Projenin kök dizininde `middleware.ts` adında bir dosya oluştur.
    Bu middleware içinde, gelen isteğin `pathname`'inin `/admin` ile başlayıp başlamadığını kontrol et.
    `src/lib/supabase/server.ts`'de oluşturulan sunucu taraflı istemciyi kullanarak `supabase.auth.getUser()` ile mevcut kullanıcı oturumunu kontrol et.
    Eğer kullanıcı giriş yapmamışsa (`user` nesnesi null ise), kullanıcıyı `/login` sayfasına yönlendir (`NextResponse.redirect`).
    Eğer kullanıcı giriş yapmışsa, isteğin devam etmesine izin ver (`NextResponse.next`).
    Middleware'in sadece ilgili rotalarda çalışması için bir `matcher` config'i ekle (örn: `/admin/:path*`).
  </action>
  <verify>
    <automated>test -e "middleware.ts" && grep -q "supabase.auth.getUser" "middleware.ts"</automated>
  </verify>
  <done>
    `/admin` rotası ve alt rotaları artık sadece giriş yapmış kullanıcılar tarafından erişilebilir.
  </done>
</task>

<task type="auto">
  <name>Task 3: Yönetici Panel Sayfasını Oluştur</name>
  <files>
    - "src/app/admin/page.tsx"
  </files>
  <action>
    `src/app/admin/page.tsx` yolunda yeni bir React Server Component oluştur.
    Bu bileşen içinde, sunucu taraflı Supabase istemcisini kullanarak tekrar `supabase.auth.getUser()` ile kullanıcı bilgilerini al.
    Kullanıcının e-posta adresini veya ID'sini ekranda gösteren basit bir "Hoş Geldiniz" mesajı oluştur.
    Plan 3'te bahsedilen çıkış yapma (logout) işlevselliği için bir buton veya link ekle. Bu, `supabase.auth.signOut()` fonksiyonunu çağıran bir istemci bileşeni olabilir.
  </action>
  <verify>
    <automated>test -e "src/app/admin/page.tsx" && grep -q "Hoş Geldiniz" "src/app/admin/page.tsx"</automated>
  </verify>
  <done>
    Giriş yapmış kullanıcılar için temel bir yönetici paneli karşılama sayfası mevcuttur.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge Network → Application Server | Middleware, gelen istekleri uygulama koduna ulaşmadan önce kenarda (edge) denetler. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Elevation of Privilege | Middleware | Mitigate | Middleware, her istekte oturumun sunucu tarafında doğrulanmasını zorunlu kılar. Bu, bir saldırganın client-side kontrolleri atlayarak korumalı rotalara yetkisiz erişimini engeller. Oturum bilgisi güvenli, httpOnly cookie'lerde saklanır. |
</threat_model>

<verification>
- Tarayıcıda (giriş yapılmamışken) `/admin` adresine gitmeye çalışıldığında otomatik olarak `/login` sayfasına yönlendirildiğini doğrula.
- Başarılı bir giriş yaptıktan sonra `/admin` adresine gidildiğinde sayfanın doğru görüntülendiğini ve kullanıcı bilgilerinin yer aldığını doğrula.
- `/admin` sayfasındaki "Çıkış Yap" butonunun çalıştığını ve kullanıcıyı anasayfaya yönlendirdiğini doğrula.
</verification>

<success_criteria>
- Yönetici paneli rotası, yetkisiz erişime karşı sunucu tarafında başarılı bir şekilde korunmaktadır.
- Yetkili kullanıcılar yönetici paneline erişebilir ve kendi bilgilerini görebilir.
</success_criteria>

<output>
After completion, create `.planning/phases/01-temel-altyapi-ve-kimlik-dogrulama/01-04-SUMMARY.md`
</output>
