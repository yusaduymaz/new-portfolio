---
phase: 01-temel-altyapi-ve-kimlik-dogrulama
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - "supabase/migrations/YYYYMMDDHHMMSS_create_profiles_table.sql"
autonomous: true
requirements: [AUTH-01]
user_setup:
  - service: Supabase CLI
    why: "The Supabase CLI is required to manage database migrations locally and deploy them to the Supabase project."
    env_vars: []
    dashboard_config:
      - task: "Install the Supabase CLI if you haven't already (e.g., `npm i -g supabase`)."
        location: "Your local terminal"
      - task: "Link the local project to your Supabase project using `supabase link --project-ref <your-project-ref>`."
        location: "Your local terminal"
      - task: "Run `supabase login` if it's your first time using the CLI."
        location: "Your local terminal"

must_haves:
  truths:
    - "A `profiles` table exists in the database."
    - "The `profiles` table is correctly linked to the `auth.users` table."
  artifacts:
    - path: "supabase/migrations/YYYYMMDDHHMMSS_create_profiles_table.sql"
      provides: "The SQL script to create the profiles table and its related policies."
      contains: "create table public.profiles"
  key_links:
    - from: "public.profiles.id"
      to: "auth.users.id"
      via: "Foreign Key constraint"
      pattern: "references auth.users"
---

<objective>
Portfolyo yöneticisi için kullanıcı profillerini saklayacak veritabanı şemasını oluşturmak.

Purpose: Bu plan, Supabase'in kimlik doğrulama sistemiyle entegre olacak temel `profiles` tablosunu oluşturur. Bu, yöneticiye özel bilgileri (kullanıcı adı, tam ad vb.) depolamak ve gelecekteki yönetici paneli özelliklerine zemin hazırlamak için gereklidir.
Output: `profiles` tablosunu oluşturan bir SQL migration dosyası.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-temel-altyapi-ve-kimlik-dogrulama/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Veritabanı Migration Dosyası Oluştur</name>
  <files>
    - "supabase/migrations/YYYYMMDDHHMMSS_create_profiles_table.sql"
  </files>
  <action>
    Supabase CLI kullanarak `create_profiles_table` adında yeni bir veritabanı migration dosyası oluştur. Bu komut (`supabase migration new create_profiles_table`), `supabase/migrations` dizininde zaman damgalı bir SQL dosyası üretecektir. Dosya adındaki `YYYYMMDDHHMMSS` kısmını, komutun ürettiği gerçek zaman damgasıyla değiştirerek çalış.
  </action>
  <verify>
    <automated>find supabase/migrations -name "*_create_profiles_table.sql" | grep .</automated>
  </verify>
  <done>
    `supabase/migrations` dizininde yeni, boş bir migration SQL dosyası oluşturulmuştur.
  </done>
</task>

<task type="auto">
  <name>Task 2: `profiles` Tablosu Şemasını Tanımla</name>
  <files>
    - "supabase/migrations/YYYYMMDDHHMMSS_create_profiles_table.sql"
  </files>
  <action>
    Oluşturulan migration dosyasına `profiles` tablosunu yaratacak SQL kodunu ekle.
    - Tablo `public.profiles` olarak adlandırılmalıdır.
    - Alanlar:
      - `id`: `uuid`, primary key olmalı ve `auth.users` tablosundaki `id`'ye referans veren bir foreign key olmalıdır. `on delete cascade` eklenmelidir.
      - `username`: `text`, `unique` olmalı.
      - `full_name`: `text`.
      - `avatar_url`: `text`.
    - Ayrıca, yeni bir kullanıcı `auth.users` tablosuna eklendiğinde, `profiles` tablosunda otomatik olarak bir satır oluşturacak bir trigger ve fonksiyon ekle. Bu, kullanıcı bilgilerini senkronize tutar.
    - Son olarak, kullanıcıların yalnızca kendi profil verilerini güncelleyebilmesi/okuyabilmesi için temel Row Level Security (RLS) politikaları ekle.
  </action>
  <verify>
    <automated>grep -q "create table public.profiles" supabase/migrations/*_create_profiles_table.sql && grep -q "references auth.users" supabase/migrations/*_create_profiles_table.sql</automated>
  </verify>
  <done>
    Migration dosyası, `profiles` tablosunu, trigger'ını ve RLS politikalarını tanımlayan SQL komutlarını içermektedir.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated User → Database | Kimliği doğrulanmış bir kullanıcının veritabanı üzerinde doğrudan (Supabase API aracılığıyla) işlem yapması. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure / Tampering | `public.profiles` | Mitigate | Row Level Security (RLS) politikaları uygulanarak kullanıcıların yalnızca kendi verilerine erişmesi ve onları değiştirmesi sağlanır. Bu, bir kullanıcının başka bir kullanıcının profil bilgilerini görmesini veya düzenlemesini engeller. |
</threat_model>

<verification>
- `supabase db push` komutu çalıştırıldığında migration'ın hatasız bir şekilde tamamlandığını doğrula.
- Supabase Studio (Dashboard) üzerinden `profiles` tablosunun ve sütunlarının doğru bir şekilde oluşturulduğunu kontrol et.
- Yeni bir kullanıcı kaydolduğunda (sonraki planlarda yapılacak), `profiles` tablosunda ilgili satırın otomatik olarak oluşturulduğunu doğrula.
</verification>

<success_criteria>
- `profiles` tablosu, belirtilen şema ile veritabanında başarıyla oluşturulmuştur.
- Kullanıcı kaydı ve profil oluşturma arasındaki senkronizasyon trigger ile sağlanmıştır.
- Temel veri erişim güvenliği RLS ile sağlanmıştır.
</success_criteria>

<output>
After completion, create `.planning/phases/01-temel-altyapi-ve-kimlik-dogrulama/01-02-SUMMARY.md`
</output>
