---
phase: 02-yonetici-paneli-ve-icerik-yonetimi
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: ["src/app/admin/layout.tsx", "src/components/admin/Sidebar.tsx", "src/app/admin/page.tsx"]
autonomous: true
requirements: ["ADM-02"]
must_haves:
  truths:
    - "Yönetici panelinde her sayfada görünen bir yan menü (sidebar) var."
    - "Dashboard ana sayfasında özet istatistikler (toplam proje vb.) gösteriliyor."
  artifacts:
    - path: "src/app/admin/layout.tsx"
      provides: "Admin layout wrapper"
    - path: "src/components/admin/Sidebar.tsx"
      provides: "Navigation sidebar"
  key_links:
    - from: "src/app/admin/layout.tsx"
      to: "src/components/admin/Sidebar.tsx"
      via: "import and render"
      pattern: "<Sidebar />"
---

<objective>
Admin Layout ve Dashboard arayüzünün oluşturulması.
Purpose: İçerik yönetim ekranları için tutarlı bir navigasyon ve genel görünüm sağlamak.
Output: Admin Layout componenti, Sidebar componenti ve güncellenmiş Dashboard sayfası.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar ve Layout Componentlerinin Oluşturulması</name>
  <files>src/components/admin/Sidebar.tsx, src/app/admin/layout.tsx</files>
  <action>
    `Sidebar` componentini oluştur (Dashboard, Projeler, Yazılar, Çıkış Yap linklerini içermeli).
    `src/app/admin/layout.tsx` dosyasını oluştur ve sayfa içeriğini `Sidebar` ile yan yana gösterecek bir düzen (flex layout) kur.
  </action>
  <verify>
    <automated>npx eslint src/app/admin/layout.tsx</automated>
  </verify>
  <done>Layout ve Sidebar başarıyla oluşturuldu ve birbirine bağlandı.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard Sayfasının Güncellenmesi</name>
  <files>src/app/admin/page.tsx</files>
  <action>
    Mevcut `src/app/admin/page.tsx` dosyasını güncelle. Sadece "Hoşgeldiniz" mesajı yerine, "Toplam Projeler" ve "Toplam Yazılar" gibi istatistik kartları ekle (şimdilik statik veri veya basit bir count sorgusu ile olabilir).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Dashboard sayfası istatistikleri gösterecek şekilde düzenlendi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Server -> UI | Layout needs to ensure only authenticated users see it. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | Admin UI | mitigate | Admin layout/page must be protected by session checks. |
</threat_model>

<verification>
/admin sayfasına gidildiğinde sidebar ve özet istatistik kartlarının görüntülendiği test edilecek.
</verification>

<success_criteria>
Kullanıcılar admin panelinde rahatça navigasyon yapabiliyor.
</success_criteria>

<output>
After completion, create `.planning/phases/02-yonetici-paneli-ve-icerik-yonetimi/02-02-SUMMARY.md`
</output>
