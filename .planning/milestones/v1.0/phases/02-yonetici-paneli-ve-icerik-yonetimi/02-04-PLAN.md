---
phase: 02-yonetici-paneli-ve-icerik-yonetimi
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: ["src/app/admin/posts/page.tsx", "src/app/admin/posts/new/page.tsx", "src/app/admin/posts/actions.ts"]
autonomous: true
requirements: ["ADM-04"]
must_haves:
  truths:
    - "Kullanıcı blog yazılarını listeleyebilir."
    - "Kullanıcı yeni yazı ekleyebilir."
  artifacts:
    - path: "src/app/admin/posts/page.tsx"
      provides: "Posts list view"
    - path: "src/app/admin/posts/actions.ts"
      provides: "Server actions for CRUD"
  key_links:
    - from: "src/app/admin/posts/page.tsx"
      to: "src/app/admin/posts/actions.ts"
      via: "form actions"
      pattern: "action="
---

<objective>
Blog/Yazı Yönetimi (CRUD) arayüzlerinin oluşturulması.
Purpose: Adminlerin portfolyoya yeni blog yazıları ekleyip yönetebilmesini sağlamak.
Output: Yazı listeleme sayfası, ekleme formu ve Server Actions.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Actions (Yazılar)</name>
  <files>src/app/admin/posts/actions.ts</files>
  <action>
    `addPost`, `deletePost`, `updatePost` gibi Supabase ile etkileşime girecek Server Action fonksiyonlarını oluştur. Bu fonksiyonlar formu alıp `posts` tablosuna işlem yapacak.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Server Actions yazıldı ve tip hataları yok.</done>
</task>

<task type="auto">
  <name>Task 2: Yazılar Listeleme ve Ekleme Arayüzü</name>
  <files>src/app/admin/posts/page.tsx, src/app/admin/posts/new/page.tsx</files>
  <action>
    `page.tsx`: Veritabanındaki yazıları çeken ve gösteren (Sil butonu ile) sayfayı oluştur.
    `new/page.tsx`: Yeni yazı eklemek için bir form (title, slug, content alanları) içeren sayfayı oluştur. Form, Task 1'deki `addPost` action'ını çağırmalı.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Sayfalar oluşturuldu ve Server Action bağlantıları yapıldı.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> Server Action | Inputs to create a post must be validated. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | Server Actions | mitigate | Check `auth.getUser()` inside server actions before modifying the database. |
</threat_model>

<verification>
Yeni bir yazı eklendiğinde listede göründüğü teyit edilecek.
</verification>

<success_criteria>
Yazılar veritabanına sorunsuz kaydediliyor ve listeleniyor.
</success_criteria>

<output>
After completion, create `.planning/phases/02-yonetici-paneli-ve-icerik-yonetimi/02-04-SUMMARY.md`
</output>
