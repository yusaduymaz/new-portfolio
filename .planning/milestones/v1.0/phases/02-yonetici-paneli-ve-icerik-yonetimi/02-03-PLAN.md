---
phase: 02-yonetici-paneli-ve-icerik-yonetimi
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: ["src/app/admin/projects/page.tsx", "src/app/admin/projects/new/page.tsx", "src/app/admin/projects/actions.ts"]
autonomous: true
requirements: ["ADM-03"]
must_haves:
  truths:
    - "Kullanıcı projeleri listeleyebilir."
    - "Kullanıcı yeni proje ekleyebilir."
  artifacts:
    - path: "src/app/admin/projects/page.tsx"
      provides: "Projects list view"
    - path: "src/app/admin/projects/actions.ts"
      provides: "Server actions for CRUD"
  key_links:
    - from: "src/app/admin/projects/page.tsx"
      to: "src/app/admin/projects/actions.ts"
      via: "form actions"
      pattern: "action="
---

<objective>
Proje Yönetimi (CRUD) arayüzlerinin oluşturulması.
Purpose: Adminlerin portfolyoya yeni projeler ekleyip yönetebilmesini sağlamak.
Output: Proje listeleme sayfası, ekleme formu ve Server Actions.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Actions (Projeler)</name>
  <files>src/app/admin/projects/actions.ts</files>
  <action>
    `addProject`, `deleteProject`, `updateProject` gibi Supabase ile etkileşime girecek Server Action fonksiyonlarını oluştur. Bu fonksiyonlar formu (FormData) alıp `projects` tablosuna insert/delete yapacak.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Server Actions yazıldı ve tip hataları yok.</done>
</task>

<task type="auto">
  <name>Task 2: Projeler Listeleme ve Ekleme Arayüzü</name>
  <files>src/app/admin/projects/page.tsx, src/app/admin/projects/new/page.tsx</files>
  <action>
    `page.tsx`: Veritabanındaki projeleri çeken ve bir tablo/liste halinde gösteren (Sil butonu ile) sayfayı oluştur.
    `new/page.tsx`: Yeni proje eklemek için bir form (title, description, URL alanları) içeren sayfayı oluştur. Form, Task 1'deki `addProject` action'ını çağırmalı.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Sayfalar oluşturuldu ve Server Action bağlantıları yapıldı.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> Server Action | Inputs to create a project must be validated. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Spoofing | Server Actions | mitigate | Check `auth.getUser()` inside server actions before modifying the database. |
</threat_model>

<verification>
Yeni bir proje eklendiğinde listede göründüğü teyit edilecek.
</verification>

<success_criteria>
Projeler veritabanına sorunsuz kaydediliyor ve listeleniyor.
</success_criteria>

<output>
After completion, create `.planning/phases/02-yonetici-paneli-ve-icerik-yonetimi/02-03-SUMMARY.md`
</output>
