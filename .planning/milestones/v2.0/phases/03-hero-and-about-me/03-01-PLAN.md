# Plan: Hero & About Me Redesign (Plan 03-01)

## Current Objective
Implement the new premium Hero layout (with macOS chrome, image zoom, and interactive `developer.json` terminal widget) and Bento Grid About Me section.

## Tasks
- [ ] Refactor `src/components/HeroSection.tsx` with the new interactive visual elements.
- [ ] Refactor the About Me section in `src/app/page.tsx` into a high-fidelity bento grid.

## Verification
- Local build checks to ensure Next.js components compile correctly.
- Test interactive "Copy Code" button in the JSON terminal widget.
- Confirm metrics display correctly on both mobile and desktop screen sizes.
