# Plan: Projeler Sayfası Revize ve Detay Modalı (Plan 01-01 — v3.0)

## Current Objective
`/projects` public sayfasını premium, tıklanabilir browser-viewport kartlarına revize etmek; projeye tıklandığında erişilebilir, kapanma tuşuna (X/Esc/backdrop) sahip detay modalını hayata geçirmek. Tüm kararlar 01-SPEC.md'deki Refero grounding'ine bağlı.

## Tasks
- [ ] 1. `src/components/projects/ProjectShowcaseCard.tsx` — tıklanabilir (button) premium browser-viewport kart.
- [ ] 2. `src/components/projects/ProjectDetailModal.tsx` — erişilebilir detail modal (Esc/backdrop/X kapatma, focus trap, scroll lock, framer-motion enter/exit).
- [ ] 3. `src/components/projects/ProjectsExplorer.tsx` — grid + modal state + URL (?p) senkronu (pushState/back/popstate).
- [ ] 4. `src/app/projects/page.tsx` — server fetch + Header/hero (WordPullUp)/Explorer/Footer yerleşimi.
- [ ] 5. `npm run lint` + `npm run build` ile derleme doğrulama; `localhost:3000/projects` manuel UAT.

## Verification
- Kart tıklayınca modal açılıyor; X / Esc / backdrop ile kapanıyor.
- Klavye: Tab kartlar arasında döner; Enter/Space kart açar; modal içinde focus trap; kapatınca focus tetikleyici karta döner.
- `?p=<id>` ile direkt link modal açıyor; geri tuşu modalı kapatıyor (sayfadan ayrılmadan).
- Boş DB durumunda zarif empty-state; eksik alanlar (image_url/content/technologies null) kırılmadan render.
- Mobil görünüm: modal alttan slide-up, kartlar tek kolon.
- Build temiz, tip hatası yok.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@DESIGN.md
@src/types/database.ts
@src/app/globals.css
@src/app/projects/page.tsx