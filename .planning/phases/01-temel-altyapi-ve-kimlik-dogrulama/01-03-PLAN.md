---
phase: 01-temel-altyapi-ve-kimlik-dogrulama
plan: 03
type: execute
wave: 3
depends_on: [01]
files_modified:
  - "src/app/login/page.tsx"
  - "src/components/auth/AuthManager.tsx"
  - "src/app/layout.tsx"
autonomous: true
requirements: [AUTH-01]
must_haves:
  truths:
    - "A user can sign in with valid credentials."
    - "A user can sign out."
    - "The application state updates reactively to authentication changes."
  artifacts:
    - path: "src/app/login/page.tsx"
      provides: "A functional login form that calls the Supabase auth API."
    - path: "src/components/auth/AuthManager.tsx"
      provides: "A client component to manage and listen for auth state changes."
  key_links:
    - from: "Login form submit handler"
      to: "supabase.auth.signInWithPassword"
      via: "Function call"
      pattern: "signInWithPassword"
    - from: "Logout button onClick handler"
      to: "supabase.auth.signOut"
      via: "Function call"
      pattern: "signOut"
---

<objective>
Kullanıcı (admin) kimlik doğrulama mantığını ve oturum yönetimini uygulamak.

Purpose: Bu plan, projenin en önemli güvenlik bileşenlerinden birini hayata geçirir. Kullanıcıların güvenli bir şekilde giriş ve çıkış yapmasını sağlar ve uygulama genelinde oturum durumunu tutarlı bir şekilde yöneterek korumalı alanların temelini oluşturur.
Output: İşlevsel bir giriş sayfası ve oturum yönetimi bileşeni.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-temel-altyapi-ve-kimlik-dogrulama/01-01-SUMMARY.md
@src/app/login/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Giriş Formunu İşlevsel Hale Getir</name>
  <files>
    - "src/app/login/page.tsx"
  </files>
  <action>
    `src/app/login/page.tsx` dosyasını "use client" direktifi ile bir istemci bileşenine dönüştür.
    Formdaki email ve şifre inputları için `useState` kullanarak state yönetimi ekle.
    Formun `onSubmit` olayını yönetecek bir fonksiyon oluştur. Bu fonksiyon içinde `event.preventDefault()` çağırarak sayfanın yeniden yüklenmesini engelle.
    Oluşturulan Supabase istemcisini (`src/lib/supabase/client.ts`) import et ve `supabase.auth.signInWithPassword` metodunu form verileriyle çağır.
    Giriş başarılı olduğunda kullanıcıyı `/admin` sayfasına yönlendir (`useRouter` hook'u kullanılabilir).
    Giriş başarısız olursa, kullanıcıya bir hata mesajı göster.
  </action>
  <verify>
    <automated>grep -q "use client" "src/app/login/page.tsx" && grep -q "signInWithPassword" "src/app/login/page.tsx"</automated>
  </verify>
  <done>
    Kullanıcılar artık `login` sayfasındaki formu kullanarak e-posta ve şifreleriyle giriş yapabilir.
  </done>
</task>

<task type="auto">
  <name>Task 2: Oturum Yönetimi ve Çıkış İşlevselliği Ekle</name>
  <files>
    - "src/components/auth/AuthManager.tsx"
    - "src/app/layout.tsx"
  </files>
  <action>
    `src/components/auth/AuthManager.tsx` adında yeni bir istemci bileşeni oluştur.
    Bu bileşen içinde, Supabase'in `onAuthStateChange` olay dinleyicisini bir `useEffect` içinde kur. Bu dinleyici, kullanıcı oturumu başladığında (SIGNED_IN) veya sonlandığında (SIGNED_OUT) tetiklenir ve uygulama state'ini günceller (örneğin bir context veya global state store aracılığıyla). Şimdilik, state değişikliğinde sadece `router.refresh()` çağırarak sunucu bileşenlerinin yeniden render olmasını tetiklemesi yeterlidir.
    Bu bileşeni ana layout dosyasına (`src/app/layout.tsx`) ekleyerek tüm sayfalarda aktif olmasını sağla.
    Ayrıca, uygulama içinde (örneğin, daha sonra eklenecek bir header veya admin panelinde) kullanılmak üzere bir "Çıkış Yap" butonu ve bu butona tıklandığında `supabase.auth.signOut` fonksiyonunu çağıran bir mantık ekle. Bu mantık şimdilik `AuthManager` içinde veya ayrı bir bileşende olabilir. Çıkış başarılı olduğunda kullanıcıyı anasayfaya ('/') yönlendir.
  </action>
  <verify>
    <automated>test -e "src/components/auth/AuthManager.tsx" && grep -q "onAuthStateChange" "src/components/auth/AuthManager.tsx"</automated>
  </verify>
  <done>
    Uygulama, kimlik doğrulama durumu değişikliklerine dinamik olarak tepki verir ve kullanıcı güvenli bir şekilde çıkış yapabilir.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input → Auth API | Kullanıcının girdiği kimlik bilgileri (e-posta/şifre) tarayıcıdan Supabase Auth servisine gönderilir. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing/Information Disclosure | Login Form | Mitigate | Şifreler Supabase'e gönderilmeden önce asla client-side'da loglanmaz veya saklanmaz. Tüm iletişim HTTPS üzerinden şifrelenir. Brute-force saldırılarına karşı koruma Supabase tarafından sağlanır (Rate Limiting). |
</threat_model>

<verification>
- Geçerli kullanıcı bilgileriyle giriş yapıldığında `/admin` sayfasına yönlendirme yapıldığını doğrula.
- Geçersiz bilgilerle giriş denendiğinde hata mesajı gösterildiğini doğrula.
- "Çıkış Yap" butonuna tıklandığında oturumun sonlandırıldığını ve anasayfaya yönlendirme yapıldığını doğrula.
- Bir sekmede giriş yapıldığında, başka bir sekmedeki uygulamanın da giriş yapmış durumu yansıttığını (veya tam tersi) doğrula.
</verification>

<success_criteria>
- Uçtan uca kimlik doğrulama akışı (giriş, oturum yönetimi, çıkış) başarıyla çalışmaktadır.
- Uygulama, kullanıcının oturum durumuna göre reaktif bir şekilde davranır.
</success_criteria>

<output>
After completion, create `.planning/phases/01-temel-altyapi-ve-kimlik-dogrulama/01-03-SUMMARY.md`
</output>
