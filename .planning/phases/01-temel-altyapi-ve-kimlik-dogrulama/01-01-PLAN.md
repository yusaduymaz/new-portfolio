---
phase: 01-temel-altyapi-ve-kimlik-dogrulama
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/lib/supabase/client.ts"
  - ".env.local"
autonomous: true
requirements: [AUTH-01]
user_setup:
  - service: Supabase
    why: "Provides the backend, database, and authentication services for the application."
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Project Dashboard -> Project Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Project Dashboard -> Project Settings -> API -> Project API Keys -> anon public"
    dashboard_config:
      - task: "Create a new Supabase project."
        location: "supabase.com/dashboard"
      - task: "Enable Email provider under Authentication -> Providers."
        location: "Supabase Project Dashboard"

must_haves:
  truths:
    - "The application can connect to the Supabase backend."
    - "API keys are securely managed and not hardcoded."
  artifacts:
    - path: "src/lib/supabase/client.ts"
      provides: "A singleton Supabase client instance for use throughout the application."
    - path: ".env.local"
      provides: "Secure storage for Supabase credentials."
  key_links:
    - from: "src/lib/supabase/client.ts"
      to: "process.env.NEXT_PUBLIC_SUPABASE_URL"
      via: "Environment variable injection at runtime."
      pattern: "createClient"
---

<objective>
Supabase projesini başlatmak ve Next.js uygulamasıyla bağlantısını yapılandırmak.

Purpose: Bu plan, projenin backend ile iletişim kurabilmesi için temel bağlantıyı kurar. Güvenli ve merkezi bir Supabase client örneği oluşturarak sonraki veritabanı ve kimlik doğrulama işlemlerinin temelini atar.
Output: Yapılandırılmış bir Supabase client modülü ve environment dosyası.
</objective>

<execution_context>
@C:/Users/duyma/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/duyma/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase İstemcisini Oluştur</name>
  <files>
    - "src/lib/supabase/client.ts"
  </files>
  <action>
    `@supabase/supabase-js` kütüphanesinden `createClient` fonksiyonunu kullanarak bir Supabase istemci örneği oluştur. Bu istemci, tarayıcıda (client-side) kullanılmak üzere tasarlanmalıdır. İstemciyi bir singleton olarak dışa aktararak uygulama genelinde aynı örneğin kullanılmasını sağla. Gerekli environment değişkenlerini (`NEXT_PUBLIC_SUPABASE_URL` ve `NEXT_PUBLIC_SUPABASE_ANON_KEY`) `process.env` üzerinden al.
  </action>
  <verify>
    <automated>test -e "src/lib/supabase/client.ts" && grep -q "createClient" "src/lib/supabase/client.ts"</automated>
  </verify>
  <done>
    `src/lib/supabase/client.ts` dosyası oluşturulmuş ve yapılandırılmış bir Supabase istemcisini dışa aktarıyor.
  </done>
</task>

<task type="auto">
  <name>Task 2: Örnek Environment Dosyasını Oluştur</name>
  <files>
    - ".env.local"
  </files>
  <action>
    Supabase bağlantısı için gerekli olan ortam değişkenlerini içeren bir `.env.local` dosyası oluştur. Bu dosya, `NEXT_PUBLIC_SUPABASE_URL` ve `NEXT_PUBLIC_SUPABASE_ANON_KEY` değişkenlerini içermeli ve değerleri boş bırakılmalıdır. Gerçek değerler, `user_setup` bölümünde belirtildiği gibi kullanıcı tarafından manuel olarak eklenecektir. Bu dosyanın `.gitignore` içinde olduğundan emin ol (Next.js varsayılan olarak ekler).
  </action>
  <verify>
    <automated>test -e ".env.local" && grep -q "NEXT_PUBLIC_SUPABASE_URL" ".env.local"</automated>
  </verify>
  <done>
    `.env.local` dosyası gerekli değişkenleri içerecek şekilde oluşturulmuştur.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Supabase API | Güvenilmeyen kullanıcı ortamından (tarayıcı) Supabase backend'ine yapılan tüm istekler bu sınırdan geçer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `client.ts` | Mitigate | Anon key kullanılır, secret key ifşa edilmez. Tüm iletişim HTTPS üzerinden yapılır (Supabase tarafından zorunlu). RLS (Row Level Security) sonraki adımlarda yapılandırılacaktır. |
</threat_model>

<verification>
- `src/lib/supabase/client.ts` dosyasının varlığını ve doğru içeriği kontrol et.
- `.env.local` dosyasının varlığını ve gerekli değişkenleri içerdiğini kontrol et.
</verification>

<success_criteria>
- Supabase client'ı başarıyla oluşturulmuş ve Next.js projesinden erişilebilir durumda.
- Ortam değişkenleri, kullanıcı tarafından doldurulmaya hazır şekilde ayarlanmış.
</success_criteria>

<output>
After completion, create `.planning/phases/01-temel-altyapi-ve-kimlik-dogrulama/01-01-SUMMARY.md`
</output>
