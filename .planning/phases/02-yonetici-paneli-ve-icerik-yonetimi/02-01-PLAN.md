---
phase: 02-yonetici-paneli-ve-icerik-yonetimi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["supabase/migrations/20260512000000_create_projects_and_posts.sql", "src/types/database.ts"]
autonomous: true
requirements: ["ADM-01"]
must_haves:
  truths:
    - "Veritabanında projeler ve yazılar için tablolar mevcut olmalı."
    - "RLS politikaları ayarlanmış olmalı."
    - "TypeScript tipleri uygulamada kullanılabilir olmalı."
  artifacts:
    - path: "supabase/migrations/20260512000000_create_projects_and_posts.sql"
      provides: "Database schema for projects and posts"
    - path: "src/types/database.ts"
      provides: "TypeScript interfaces"
  key_links:
    - from: "src/types/database.ts"
      to: "Supabase"
      via: "Type definitions"
      pattern: "export interface Project"
---

<objective>
Veritabanı Şeması ve Typescript Arayüzlerinin oluşturulması.
Purpose: İçerik yönetiminin temelini oluşturan veri yapılarını hazırlamak.
Output: Supabase migration dosyası ve TS tip tanımlamaları.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase Migration Dosyası Oluşturma</name>
  <files>supabase/migrations/20260512000000_create_projects_and_posts.sql</files>
  <action>
    Proje (projects) ve Yazı (posts) tablolarını oluşturacak SQL migration dosyasını yaz. 
    `projects` tablosunda id, title, description, image_url, live_url, github_url, created_at olmalı.
    `posts` tablosunda id, title, slug, content, published, created_at, updated_at olmalı.
    Her iki tablo için de RLS (Row Level Security) aktif edilmeli. Sadece adminlerin (veya auth olan kullanıcıların) insert/update/delete yapmasına, herkesin okumasına izin veren politikalar eklenmeli.
  </action>
  <verify>
    <automated>npx supabase migration list</automated>
  </verify>
  <done>Migration dosyası hatasız çalışacak şekilde hazırlandı.</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript Tiplerini Tanımlama</name>
  <files>src/types/database.ts</files>
  <action>
    Oluşturulan `projects` ve `posts` tabloları için TypeScript interface'lerini `src/types/database.ts` dosyasına ekle. 
    Örneğin: `export interface Project { id: string; title: string; ... }`
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Tipler başarıyla dışa aktarıldı (exported) ve TypeScript derlemesi başarılı.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Database -> App | Database schema and RLS defines who can modify content. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Supabase DB | mitigate | Use RLS so only authenticated admin users can modify projects/posts. |
</threat_model>

<verification>
Migration dosyasının veritabanında çalıştırılıp tabloların oluşturulduğu doğrulanacak.
</verification>

<success_criteria>
Tablolar tanımlandı, TypeScript tipleri projede import edilebilir durumda.
</success_criteria>

<output>
After completion, create `.planning/phases/02-yonetici-paneli-ve-icerik-yonetimi/02-01-SUMMARY.md`
</output>
